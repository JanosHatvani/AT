<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>AT.Infrastructure</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\AT.Core\AT.Core.csproj" />
  </ItemGroup>

  <!--
    Ide kerülnek majd a következő körben:
    - Microsoft.Extensions.Configuration (appsettings.json, hardcode-olt path-ok kiváltása)
    - Serilog (a 102 MessageBox.Show helyett strukturált logolás)
    - Microsoft.EntityFrameworkCore.Sqlite (a jelenlegi SQL.cs helyett)
  -->

</Project>
