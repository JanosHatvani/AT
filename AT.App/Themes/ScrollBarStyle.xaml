<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        Egyedi, minimál görgetősáv-stílus — a StaticResource brush-ok (Brush.Surface,
        Brush.Border, Brush.Accent) a projekt meglévő téma-erőforrásaira hivatkoznak,
        ugyanazokra, amiket a többi vezérlő is használ. Ha nálad más néven vannak
        definiálva ezek a brush-ok, a fájl tetején lévő 4 sort (ScrollBar.TrackBrush,
        ScrollBar.ThumbBrush, ScrollBar.ThumbHoverBrush, ScrollBar.ThumbDragBrush)
        könnyen átírhatod a saját elnevezésedre.

        Viselkedés:
        - A sáv alapból vékony (8px) és félig áttetsző, csak a "csík" (thumb) látszik,
          nincs külön nyíl-gomb / háttér-sín — modern, minimál hatás.
        - Hoverre a thumb kicsit szélesedik és felragyog (Brush.Accent-re vált).
        - Húzás közben a legerősebb (Brush.Accent, teljes opacitás) árnyalatot kapja.
        - Vízszintes és függőleges sávra egyaránt vonatkozik.
    -->

    <SolidColorBrush x:Key="ScrollBar.TrackBrush" Color="Transparent" />
    <SolidColorBrush x:Key="ScrollBar.ThumbBrush" Color="#4A4A5C" />
    <SolidColorBrush x:Key="ScrollBar.ThumbHoverBrush" Color="#00E38A" Opacity="0.65" />
    <SolidColorBrush x:Key="ScrollBar.ThumbDragBrush" Color="#00E38A" />

    <!-- A fel/le (vagy balra/jobbra) nyílgombokat szándékosan 0 méretűre/láthatatlanra
         állítjuk — csak a húzható "csík" (Thumb) marad látható, ez adja a minimál hatást. -->
    <Style x:Key="ScrollBarLineButton" TargetType="RepeatButton">
        <Setter Property="Width" Value="0" />
        <Setter Property="Height" Value="0" />
        <Setter Property="Visibility" Value="Collapsed" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Rectangle Fill="Transparent" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Az üres sín-terület (PageUp/PageDown gombok, amikre a thumb-on kívülre kattintva
         ugrik a görgetés) — átlátszó, csak a kattintható terület miatt kell. -->
    <Style x:Key="ScrollBarPageButton" TargetType="RepeatButton">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RepeatButton">
                    <Rectangle Fill="{StaticResource ScrollBar.TrackBrush}" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Maga a húzható "csík" — lekerekített, animált szélesség-/szín-váltással hoverre/húzásra. -->
    <Style x:Key="ScrollBarThumbVertical" TargetType="Thumb">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border x:Name="ThumbBorder"
                            Background="{StaticResource ScrollBar.ThumbBrush}"
                            CornerRadius="4"
                            Margin="2,0,2,0"
                            Width="20"
                            HorizontalAlignment="Center">
                        <Border.RenderTransform>
                            <ScaleTransform x:Name="ThumbScale" CenterX="0.5" ScaleX="1" />
                        </Border.RenderTransform>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="ThumbBorder"
                                                        Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                        To="#00E38A" Duration="0:0:0.12" />
                                        <DoubleAnimation Storyboard.TargetName="ThumbBorder"
                                                         Storyboard.TargetProperty="Width"
                                                         To="9" Duration="0:0:0.12" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="ThumbBorder"
                                                        Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                        To="#4A4A5C" Duration="0:0:0.15" />
                                        <DoubleAnimation Storyboard.TargetName="ThumbBorder"
                                                         Storyboard.TargetProperty="Width"
                                                         To="6" Duration="0:0:0.15" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="ThumbBorder" Property="Background" Value="{StaticResource ScrollBar.ThumbDragBrush}" />
                            <Setter TargetName="ThumbBorder" Property="Width" Value="9" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ScrollBarThumbHorizontal" TargetType="Thumb">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border x:Name="ThumbBorder"
                            Background="{StaticResource ScrollBar.ThumbBrush}"
                            CornerRadius="4"
                            Margin="0,2,0,2"
                            Height="6"
                            VerticalAlignment="Center">
                        <Border.RenderTransform>
                            <ScaleTransform x:Name="ThumbScale" CenterY="0.5" ScaleY="1" />
                        </Border.RenderTransform>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="ThumbBorder"
                                                        Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                        To="#00E38A" Duration="0:0:0.12" />
                                        <DoubleAnimation Storyboard.TargetName="ThumbBorder"
                                                         Storyboard.TargetProperty="Height"
                                                         To="9" Duration="0:0:0.12" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <ColorAnimation Storyboard.TargetName="ThumbBorder"
                                                        Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                                                        To="#4A4A5C" Duration="0:0:0.15" />
                                        <DoubleAnimation Storyboard.TargetName="ThumbBorder"
                                                         Storyboard.TargetProperty="Height"
                                                         To="6" Duration="0:0:0.15" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="ThumbBorder" Property="Background" Value="{StaticResource ScrollBar.ThumbDragBrush}" />
                            <Setter TargetName="ThumbBorder" Property="Height" Value="9" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Függőleges görgetősáv teljes sablonja -->
    <ControlTemplate x:Key="VerticalScrollBarTemplate" TargetType="ScrollBar">
        <Grid Background="{StaticResource ScrollBar.TrackBrush}" Width="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="0" />
                <RowDefinition Height="*" />
                <RowDefinition Height="0" />
            </Grid.RowDefinitions>

            <RepeatButton Grid.Row="0" Style="{StaticResource ScrollBarLineButton}" Command="ScrollBar.LineUpCommand" />

            <Track x:Name="PART_Track" Grid.Row="1" IsDirectionReversed="True">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageUpCommand" />
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource ScrollBarThumbVertical}" />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageDownCommand" />
                </Track.IncreaseRepeatButton>
            </Track>

            <RepeatButton Grid.Row="2" Style="{StaticResource ScrollBarLineButton}" Command="ScrollBar.LineDownCommand" />
        </Grid>
    </ControlTemplate>

    <!-- Vízszintes görgetősáv teljes sablonja -->
    <ControlTemplate x:Key="HorizontalScrollBarTemplate" TargetType="ScrollBar">
        <Grid Background="{StaticResource ScrollBar.TrackBrush}" Height="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="0" />
            </Grid.ColumnDefinitions>

            <RepeatButton Grid.Column="0" Style="{StaticResource ScrollBarLineButton}" Command="ScrollBar.LineLeftCommand" />

            <Track x:Name="PART_Track" Grid.Column="1">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageLeftCommand" />
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource ScrollBarThumbHorizontal}" />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Style="{StaticResource ScrollBarPageButton}" Command="ScrollBar.PageRightCommand" />
                </Track.IncreaseRepeatButton>
            </Track>

            <RepeatButton Grid.Column="2" Style="{StaticResource ScrollBarLineButton}" Command="ScrollBar.LineRightCommand" />
        </Grid>
    </ControlTemplate>

    <!-- Kulcs NÉLKÜLI (implicit) Style — ez teszi automatikussá, hogy MINDEN ScrollBar
         (bármelyik ScrollViewer-en, listán, nézeten belül, az egész alkalmazásban)
         ezt a designt kapja, anélkül hogy bárhol külön hivatkozni kellene rá. -->
    <Style TargetType="ScrollBar">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Vertical">
                <Setter Property="Width" Value="10" />
                <Setter Property="Template" Value="{StaticResource VerticalScrollBarTemplate}" />
            </Trigger>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Height" Value="10" />
                <Setter Property="Template" Value="{StaticResource HorizontalScrollBarTemplate}" />
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
