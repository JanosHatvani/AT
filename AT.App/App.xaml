<Application x:Class="AT.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:AT.App.ViewModels"
             xmlns:views="clr-namespace:AT.App.Views"
             xmlns:conv="clr-namespace:AT.App.Converters"
             ShutdownMode="OnMainWindowClose">

    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/Colors.xaml" />
                <ResourceDictionary Source="Themes/Controls.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <conv:InverseBooleanConverter x:Key="InverseBooleanConverter" />

            <!-- ViewModel-first navigáció: a MainWindow csak a ViewModelt ismeri,
                 ez a leképezés dönti el, melyik View jelenik meg hozzá. -->
            <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
                <views:DashboardView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:WebTestViewModel}">
                <views:WebTestView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:DesktopTestViewModel}">
                <views:DesktopTestView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:MobileTestViewModel}">
                <views:MobileTestView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:HistoryViewModel}">
                <views:HistoryView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:ScheduledTasksViewModel}">
                <views:ScheduledTasksView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
                <views:SettingsView />
            </DataTemplate>

        </ResourceDictionary>
    </Application.Resources>

</Application>
